// Copy this file to config.js and fill in your Supabase project's values.
// The anon key is meant to be public (it is exposed in page source on any
// Supabase web app) — access control is enforced by RLS policies in
// supabase/schema.sql, not by keeping this key secret.
window.NMPP_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-ANON-OR-PUBLISHABLE-KEY",
};
